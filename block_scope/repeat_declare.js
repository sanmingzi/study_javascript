var count = 1;
// let count = 1; // Identifier 'count' has already been declared
// const count = 1; // Identifier 'count' has already been declared

let letVariable = 1;
// var letVariable = 2; // Identifier 'letVariable' has already been declared
console.log(letVariable);

let condition = true;
if(condition) {
  let letVariable = true;
  console.log(letVariable);
} else {
  let letVariable = false;
  console.log(letVariable);
}
